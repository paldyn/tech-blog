---
import '../styles/global.css';
import 'katex/dist/katex.min.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ScrollTopButton from '../components/ScrollTopButton.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  /** 경로만으로 판정되지 않는 화면에서 켤 내비 항목 (예: 글 상세) */
  activeNav?: string;
  /** 글 상세에서만 AI 질문 버튼과 Go to Top의 자리를 함께 잡습니다. */
  hasArticleAsk?: boolean;
}

const { title, description = SITE_DESCRIPTION, ogImage, activeNav, hasArticleAsk = false } = Astro.props;
const pageTitle = title ? `${title} — ${SITE_TITLE}` : SITE_TITLE;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="generator" content={Astro.generator} />
  <title>{pageTitle}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonicalURL} />
  <link rel="icon" type="image/x-icon" href={`${base}favicon.ico`} sizes="16x16 32x32 48x48 64x64 128x128 256x256" />
  {/* SVG를 아는 브라우저는 이쪽을 쓴다 — 어느 배율에서도 다시 그린다. */}
  <link rel="icon" type="image/svg+xml" href={`${base}icon.svg`} />
  <link rel="alternate" type="application/rss+xml" title={SITE_TITLE} href={`${base}rss.xml`} />

  <meta property="og:type" content="website" />
  <meta property="og:title" content={pageTitle} />
  <meta property="og:description" content={description} />
  <meta property="og:url" content={canonicalURL} />
  {ogImage && <meta property="og:image" content={new URL(ogImage, Astro.site)} />}

  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={pageTitle} />
  <meta name="twitter:description" content={description} />

  <script is:inline>
    (function () {
      try {
        var t = localStorage.getItem('theme');
        if (!t) {
          t = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        }
        document.documentElement.setAttribute('data-theme', t);
      } catch (e) {}
    })();
  </script>
</head>
<body class:list={{ 'has-article-ai': hasArticleAsk }}>
  <Header activeNav={activeNav} />
  <main>
    <slot />
  </main>
  <Footer />
  <ScrollTopButton hasArticleAsk={hasArticleAsk} />
</body>
</html>
